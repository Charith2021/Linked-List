import ds.Node;

public class SinglyTest {
    public static void main(String[] args) {
        Node node=new Node(10);
       // node.apppend();
       // node.prepend();
        //node.deleteWithValue();
    }
}
